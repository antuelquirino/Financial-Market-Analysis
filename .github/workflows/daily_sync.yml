name: Daily Financial Data Sync

on:
  schedule:
    - cron: '0 0 * * *' 
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install pandas yfinance google-cloud-bigquery pandas-gbq db-dtypes

      - name: Authenticate to Google Cloud
        shell: bash
        run: |
          # Usamos un método más seguro para escribir el archivo sin romper el JSON
          cat <<EOF > gcp_key.json
          ${{ secrets.GCP_SA_KEY }}
          EOF
          echo "GOOGLE_APPLICATION_CREDENTIALS=$GITHUB_WORKSPACE/gcp_key.json" >> $GITHUB_ENV

      - name: Run Daily Price Extraction
        run: python extraction/daily_update.py